langcode: en
status: true
dependencies:
  config:
    - group.type.default
  module:
    - user
id: default-group_membership
label: 'Default: Group membership'
description: 'Adds users to groups as members.'
group_type: default
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
